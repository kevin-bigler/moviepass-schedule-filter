// TODO: get the /movies page and parse the days and movies under it
import '@babel/polyfill';
import getSchedule from './getSchedule.sandbox';

getSchedule();


document.getElementById('main').innerHTML('a winner is you');